/**
 * API entrypoint — skeleton only.
 *
 * Sprint 1 delivers the tenancy spine, not endpoints. The HTTP surface (tRPC for
 * the first-party app, REST for partners) lands from sprint 7 alongside
 * onboarding. The contract is specified in docs/03-engineering/api-spec.md.
 */
export { bootstrap } from './startup.js';
